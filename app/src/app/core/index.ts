// Core module barrel export.
// Singleton services, guards, and interceptors are exported from here.
// Do NOT export components from the core barrel — components belong in shared/ or features/.
export * from './services/api.service';
export * from './services/auth.service';
export * from './guards/auth.guard';
